import "./test.wp" as test

GET /test-import
  |> pipeline: test::one
  |> jq: `.`

describe "import test"
  it "gets one"
    when calling GET /test-import
    then status is 200
    and output `.one` equals "one"
